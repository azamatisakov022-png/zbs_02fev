spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eco_operator_dev
    username: eco_operator
    password: eco_operator

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

jwt:
  secret: dev-secret-key-only-for-local-development-do-not-use-in-production

logging:
  level:
    kg.eco.operator: DEBUG
    kg.eco.operator.integration: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
